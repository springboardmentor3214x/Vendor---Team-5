import { HttpClient } from '@angular/common/http'; import { Injectable, inject } from '@angular/core'; import { Observable } from 'rxjs'; import { environment } from '../../../environments/environment';
export interface Certification { id: number; vendor_id: number; certification_name: string; certificate_number?: string | null; issuing_authority?: string | null; issue_date?: string | null; expiry_date?: string | null; document_url?: string | null; status?: string | null; }
@Injectable({providedIn:'root'}) export class CertificationService { private http=inject(HttpClient); private base=`${environment.apiUrl}/certifications`; createCertification(payload: Partial<Certification>):Observable<Certification>{return this.http.post<Certification>(`${this.base}/`,payload)} getVendorCertifications(vendorId:number):Observable<Certification[]>{return this.http.get<Certification[]>(`${this.base}/vendor/${vendorId}`)} updateCertification(id:number,payload:Partial<Certification>):Observable<Certification>{return this.http.patch<Certification>(`${this.base}/${id}`,payload)} deleteCertification(id:number):Observable<void>{return this.http.delete<void>(`${this.base}/${id}`)} }
