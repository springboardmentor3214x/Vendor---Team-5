import { HttpClient } from '@angular/common/http'; import { Injectable, inject } from '@angular/core'; import { Observable } from 'rxjs'; import { environment } from '../../../environments/environment';
export interface ComplianceRecord { id:number; vendor_id:number; status?:string; compliance_status?:string; verified_at?:string; [key:string]:unknown; }
@Injectable({providedIn:'root'}) export class ComplianceService { private http=inject(HttpClient); private base=`${environment.apiUrl}/compliance`; verifyCompliance(payload:Record<string,unknown>):Observable<ComplianceRecord>{return this.http.post<ComplianceRecord>(`${this.base}/verify`,payload)} getVendorCompliance(vendorId:number):Observable<ComplianceRecord[]>{return this.http.get<ComplianceRecord[]>(`${this.base}/vendor/${vendorId}`)} updateComplianceStatus(id:number,status:string):Observable<ComplianceRecord>{return this.http.patch<ComplianceRecord>(`${this.base}/${id}/status`,{status})} getDashboard():Observable<Record<string,unknown>>{return this.http.get<Record<string,unknown>>(`${environment.apiUrl}/dashboard`)} }
