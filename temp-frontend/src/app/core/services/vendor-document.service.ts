import { HttpClient } from '@angular/common/http'; import { Injectable, inject } from '@angular/core'; import { Observable } from 'rxjs'; import { environment } from '../../../environments/environment';
export interface VendorDocument { id:number; vendor_id:number; document_type:string; file_name?:string; uploaded_at?:string; [key:string]:unknown; }
@Injectable({providedIn:'root'}) export class VendorDocumentService { private http=inject(HttpClient); private base=`${environment.apiUrl}/documents`; listVendorDocuments(vendorId:number):Observable<VendorDocument[]>{return this.http.get<VendorDocument[]>(`${this.base}/vendor/${vendorId}`)} getDocument(id:number):Observable<VendorDocument>{return this.http.get<VendorDocument>(`${this.base}/${id}`)} downloadDocument(id:number):Observable<Blob>{return this.http.get(`${this.base}/${id}/download`,{responseType:'blob'})} uploadDocument(vendorId:number,documentType:string,file:File):Observable<VendorDocument>{const form=new FormData();form.append('vendor_id',String(vendorId));form.append('document_type',documentType);form.append('file',file);return this.http.post<VendorDocument>(`${this.base}/upload`,form)} updateDocumentMetadata(id:number,documentType:string):Observable<VendorDocument>{return this.http.patch<VendorDocument>(`${this.base}/${id}`,{documentType})} deleteDocument(id:number):Observable<void>{return this.http.delete<void>(`${this.base}/${id}`)} }
