import { CommonModule, DatePipe } from '@angular/common'; import { Component, inject, signal } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { ActivityLog, CommunicationService, Discussion, Message } from '../../core/services/communication.service';
@Component({selector:'app-communication-hub',standalone:true,imports:[CommonModule,FormsModule,DatePipe],template:`<section class="page-card"><header><div><p class="eyebrow">Module 7 · Communication</p><h2>Communication workspace</h2><p>Secure messages, procurement discussions, and an audit trail.</p></div><button class="btn" (click)="load()">Refresh</button></header><p *ngIf="error()" class="alert alert-danger">{{error()}}</p><div class="grid"><article><h3>Send a message</h3><form (ngSubmit)="send()"><input class="form-control" required type="number" [(ngModel)]="vendorId" name="vendor" placeholder="Vendor ID"><input class="form-control" [(ngModel)]="subject" name="subject" placeholder="Subject"><textarea class="form-control" required [(ngModel)]="message" name="message" placeholder="Write a business message"></textarea><button class="btn btn-primary">Send message</button></form></article><article><h3>Create discussion</h3><form (ngSubmit)="createDiscussion()"><input class="form-control" required [(ngModel)]="title" name="title" placeholder="Discussion title"><input class="form-control" type="number" [(ngModel)]="vendorId" name="discussionVendor" placeholder="Vendor ID"><textarea class="form-control" [(ngModel)]="initialMessage" name="initial" placeholder="Opening message"></textarea><button class="btn btn-primary">Create discussion</button></form></article></div><div class="grid"><article><h3>Messages</h3><p *ngIf="!messages().length">No messages yet.</p><div class="item" *ngFor="let item of messages()"><strong>{{item.subject || 'Message'}}</strong><p>{{item.message}}</p><small>{{item.createdAt|date:'medium'}}</small></div></article><article><h3>Discussion history</h3><p *ngIf="!discussions().length">No discussions yet.</p><div class="item" *ngFor="let item of discussions()"><strong>{{item.title}}</strong><p>{{item.status}} · {{item.participantCount || 0}} participants</p><small>{{item.createdAt|date:'medium'}}</small></div></article></div><article><h3>Activity log</h3><div class="item" *ngFor="let item of logs()"><strong>{{item.action}}</strong> — {{item.description || item.module}} <small>{{item.createdAt|date:'medium'}}</small></div></article></section>`,styles:[`.eyebrow{color:var(--vr-teal);font-weight:700;text-transform:uppercase}header,.grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:1rem;margin-bottom:1rem}header{display:flex;justify-content:space-between}.grid article,article{border:1px solid var(--vr-border);border-radius:12px;padding:1rem}.item{border-top:1px solid var(--vr-border);padding:.7rem 0}.item p{margin:.3rem 0}form{display:grid;gap:.6rem}textarea{min-height:84px}@media(max-width:700px){.grid{grid-template-columns:1fr}}`]}) export class CommunicationHubComponent {private api=inject(CommunicationService);messages=signal<Message[]>([]);discussions=signal<Discussion[]>([]);logs=signal<ActivityLog[]>([]);error=signal('');vendorId=1;subject='';message='';title='';initialMessage='';ngOnInit(){this.load()}load(){this.api.messages().subscribe({next:x=>this.messages.set(x),error:()=>this.error.set('Unable to load communication data.')});this.api.discussions().subscribe({next:x=>this.discussions.set(x)});this.api.logs().subscribe({next:x=>this.logs.set(x)})}send(){this.api.send({vendorId:this.vendorId,subject:this.subject,message:this.message}).subscribe({next:()=>{this.subject='';this.message='';this.load()},error:()=>this.error.set('Unable to send message.')})}createDiscussion(){this.api.createDiscussion({title:this.title,vendorId:this.vendorId,initialMessage:this.initialMessage}).subscribe({next:()=>{this.title='';this.initialMessage='';this.load()},error:()=>this.error.set('Unable to create discussion.')})}}
