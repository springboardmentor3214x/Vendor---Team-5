import { CommonModule, DatePipe } from '@angular/common'; import { Component, inject, signal } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { CommunicationService, Message } from '../../core/services/communication.service';
@Component({selector:'app-messages',standalone:true,imports:[CommonModule,FormsModule,DatePipe],template:`<section class="messages"><header><div><span class="eyebrow">Module 7</span><h2>💬 Messages</h2></div><button class="notice">🔔 System Notifications ({{unread()}})</button></header><main><aside><h3>Contacts</h3><input class="form-control" type="number" [(ngModel)]="vendorId" (change)="load()" placeholder="Vendor ID"><p>Vendor {{vendorId}} conversation</p><button class="btn" (click)="load()">Refresh conversation</button></aside><article class="chat"><div class="chat-head"><strong>Vendor conversation</strong><span>● Online</span></div><div class="thread"><p *ngIf="!messages().length">No messages yet. Start the conversation below.</p><div class="bubble" *ngFor="let item of messages()"><b>{{item.subject || 'Message'}}</b><p>{{item.message}}</p><small>{{item.createdAt|date:'shortTime'}}</small></div></div><div class="attachments" *ngIf="files().length"><span *ngFor="let file of files()">📎 {{file.filename}}</span></div><form (ngSubmit)="send()"><input class="form-control" [(ngModel)]="subject" name="subject" placeholder="Subject"><textarea class="form-control" required [(ngModel)]="message" name="message" placeholder="Type a message..."></textarea><input type="file" (change)="pick($event)"><button class="btn btn-primary">➤ Send</button></form></article></main></section>`,styles:[`.messages{color:#172033}.messages header{display:flex;justify-content:space-between;align-items:center;margin-bottom:1rem}.eyebrow{color:#7c3aed;font-weight:800}.notice{padding:.7rem 1rem;border:1px solid #cbd5e1;border-radius:8px;background:#f8fafc;font-weight:700}main{display:grid;grid-template-columns:280px 1fr;border:1px solid #dbe3ee;min-height:560px;background:#fff}aside{padding:1rem;border-right:1px solid #dbe3ee;background:#f8fbff}aside>*{margin-bottom:1rem}.chat{display:flex;flex-direction:column}.chat-head{padding:1rem;border-bottom:1px solid #dbe3ee;display:flex;justify-content:space-between}.chat-head span{color:#16a34a}.thread{padding:1rem;flex:1;background:#f4fbfb}.bubble{background:white;border-radius:12px;padding:.8rem 1rem;margin:.8rem 0;box-shadow:0 1px 4px #cbd5e166}.bubble p{margin:.35rem 0}.bubble small{color:#64748b}.attachments{padding:.6rem 1rem;background:#fff7ed}.attachments span{margin-right:1rem}form{display:grid;grid-template-columns:1fr auto;gap:.6rem;padding:1rem;border-top:1px solid #dbe3ee}textarea{grid-column:1/-1;min-height:70px}@media(max-width:700px){main{grid-template-columns:1fr}aside{border-right:0;border-bottom:1px solid #dbe3ee}.messages header{align-items:flex-start;flex-direction:column;gap:.6rem}}`]}) export class MessagesComponent {private api=inject(CommunicationService);vendorId=1;subject='';message='';file:File|null=null;messages=signal<Message[]>([]);files=signal<{id:number;filename:string;createdAt:string}[]>([]);unread=signal(0);ngOnInit(){this.load();this.api.unreadNotifications().subscribe({next:x=>this.unread.set(x.unreadCount)})}load(){this.api.messages(this.vendorId).subscribe({next:x=>this.messages.set(x)});this.api.files(this.vendorId).subscribe({next:x=>this.files.set(x)})}pick(event:Event){this.file=(event.target as HTMLInputElement).files?.[0]||null}send(){this.api.send({vendorId:this.vendorId,subject:this.subject,message:this.message}).subscribe({next:()=>{if(this.file)this.api.upload(this.file,this.vendorId).subscribe({next:()=>this.load()});this.subject='';this.message='';this.file=null;this.load()}})}}
